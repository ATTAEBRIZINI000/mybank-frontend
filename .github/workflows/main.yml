name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
    - name: Login to Docker Hub
      run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
    - name: Build The Services
      run: docker build -t laouniabderrazzaq/bankfront .
    - name: Push to docker hub
      run: docker push laouniabderrazzaq/bankfront:latest
  deploy:
    needs: build
    runs-on: self-hosted
    steps:
    - name: Check Docker Daemon
      run: systemctl status docker
    - name: Stop Container 
      run: docker stop bankfront || true
    - name: Remove Container
      run: docker rm bankfront || true
    - name: Remove Image
      run: docker rmi laouniabderrazzaq/bankfront:latest || true
    - name: Pull Image
      run: docker pull laouniabderrazzaq/bankfront:latest
    - name: Run Container
      run: docker run -d --name bankfront -p 3000:80 laouniabderrazzaq/bankfront:latest

